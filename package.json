{
  "name": "tkdu-site",
  "version": "1.0.0",
  "description": "TKD University — Tailwind CSS build for Jekyll/Netlify",
  "scripts": {
    "build": "tailwindcss -i ./src/css/input.css -o ./assets/css/main.css --minify",
    "watch": "tailwindcss -i ./src/css/input.css -o ./assets/css/main.css --watch"
  },
  "devDependencies": {
    "tailwindcss": "^3.4.0"
  }
}
