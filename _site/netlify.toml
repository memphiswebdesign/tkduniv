[build]
  command = "bundle install --path vendor/bundle && bundle exec jekyll build"
  publish = "_site"

[build.environment]
  BUNDLE_WITHOUT = "development:test"

[build.environment]
  BUNDLER_VERSION = "2.6.9"